import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 10,
    backgroundColor: '#fff',
  },
  summeryStyle: {
    fontSize: 16,
    color: '#000',
    marginTop: 10,
  },
  dateStyle: {
    fontSize: 18,
    color: '#000',
  },
});
